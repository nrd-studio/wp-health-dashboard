# Changelog

このプロジェクトの変更履歴です。バージョン番号は WP Health Agent プラグインに準拠します。

## [1.3.0] - 2026-06-10

### 追加

- **コア自動更新ポリシーの検知** — 新エンドポイント `GET /auto-update` を追加。WordPress コアと同じ優先順位（`AUTOMATIC_UPDATER_DISABLED` 定数 → `WP_AUTO_UPDATE_CORE` 定数 → DB オプション → フィルター）でメジャー／マイナー自動更新の有効状態を判定し、ダッシュボードに「🛡 コア自動更新ポリシーバー」として表示。設定の出どころ（wp-config.php 定数 / 管理画面設定 / WordPress デフォルト / フィルター上書き）も表示します。
- **メジャー自動更新の遠隔 OFF** — 新エンドポイント `POST /auto-update/major` を追加。ダッシュボードからメジャーバージョン（例: 6.9 → 7.0）への自動更新をワンクリックで無効化できます。OFF にしてもマイナー更新（セキュリティ修正）は自動適用が維持されます。wp-config.php の定数で制御されているサイトでは 409 エラーで変更を拒否し、ボタンも無効化されます（「OFF にしたつもりが効いていない」誤認の防止）。
- **意図しないバージョンアップの検知** — 前回スキャン時の WordPress バージョンをブラウザの localStorage に記録し、再スキャン時に変化していた場合は警告バナーを表示します（自動更新やホスティング側更新の発見用）。
- **健康スコアの判定強化** — メジャー自動更新が ON のサイトはスコア −15 点。
- 旧バージョンのプラグイン（v1.2 以前）に接続した場合、ポリシーバーにプラグイン更新の案内を表示。

### セキュリティ

- **API キーの送信方法を変更** — URL クエリパラメータから `X-WPH-Key` ヘッダ送信に変更（アクセスログへのキー残留対策）。プロキシはヘッダ優先で読み取り、クエリも後方互換として受理します。
- **プロキシの SSRF 対策** — `https://` 以外の URL を拒否、プライベート IP 帯への接続を遮断、パスをホワイトリストで検証、URL から origin 以外のセグメントを除去、リダイレクトを追跡しない（`redirect: 'manual'`）よう変更。

### 変更

- 確認モーダルを汎用化（タイトル・OK ボタンのラベルを用途別に変更可能に）。
- プロキシを CommonJS + `https` モジュールから ESM + `fetch` ベースに書き換え。
- リダイレクト応答を受けた場合、原因（siteUrl の www 有無・https の不一致）が分かるエラーメッセージを返すよう変更。

### 修正

- プラグインの PHP 7.4 互換性を維持（union 型宣言を不使用に修正）。古い PHP で動くサイトこそ監視対象のため。

### 互換性に関する注意

- プラグイン v1.2 以前 × 新ダッシュボード: 基本機能は動作しますが、自動更新ポリシー機能は使えません。
- siteUrl は https 必須・リダイレクト不可になったため、www の有無を含む正規 URL での入力が必要です。

## [1.2.0]

### 修正

- CORS 問題を修正（OPTIONS プリフライトへの応答、`rest_pre_serve_request` でのヘッダ付与）。

## [1.0.0]

### 追加

- 初回リリース。
- WordPress / PHP / プラグインのバージョン・更新状況の取得（`GET /status`）。
- テーマ PHP ファイルの取得とリスク分析（`GET /theme-files`）— PHP 8.x 非互換パターンの静的解析、テーマ内のプラグイン依存箇所の検出。
- WordPress 本体・プラグインの更新実行（`POST /update/core`, `POST /update/plugin`, `POST /update/all`）。
- 更新前リスク分析モーダル、健康スコア表示。
- 管理画面の API キー生成ページ。
